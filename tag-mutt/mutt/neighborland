# -*- mode: conf; -*-
set postponed = "+neighborland/drafts"
set from = "scott@neighborland.com"
color status green default
unset record

macro index,pager "#" "<save-message>+neighborland/trash<enter>" \
    "move message to the trash"

macro index,pager S "<save-message>+neighborland/spam<enter>"  \
        "mark message as spam"

macro index "<esc><tab>" "<change-folder>+scottericjacobsen/inbox<enter>" "To personal"
